{% extends '::base.html.twig' %}

{% block body %}

    {% if title|default('') %}
    <h1>{{ title }}</h1><hr>
    {% endif %}

    {{ form_start(form, {'style':'horizontal'}) }}
    {{ form_end(form) }}

{% endblock %}